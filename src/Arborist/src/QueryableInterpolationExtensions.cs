namespace Arborist;

/// <summary>
/// Provides interpolating overloads for extension methods on <see cref="IQueryable{T}"/> defined
/// by <see cref="Queryable"/>.
/// </summary>
public static partial class QueryableInterpolationExtensions {
    // Extension method implementations are provided by the accompanying source generator.
    // This is slightly janky, but the only realistic way to ensure that all of the generated
    // overloads are correct. In addition, it lets us automatically track the Queryable methods
    // defined by the target platform.
}
